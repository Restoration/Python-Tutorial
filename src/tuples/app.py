# Tuples is Immutable
# tuples syntax defferents list
coordinates = (4, 5)
# coordinates[0] = 10 # not working
print(coordinates[1])

# Also you can use tuples inside list
coordinates2 = [(4, 5), (6, 7), (80, 34)]
